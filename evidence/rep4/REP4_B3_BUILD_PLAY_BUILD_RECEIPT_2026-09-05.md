# Rep4 B3 BUILD → PLAY → BUILD receipt — 2026-09-05

Status: **BOUNDED MACHINE / RENDERED PASS — OWNER VERDICT NOT YET RUN**

## Qualified runtime

- implementation branch: `experiment/rep4-b3-build-play-build`
- exact qualified runtime SHA: `c852132ba152791867363925baadbfd08c14840c`
- frozen machine checkpoint: `rep4-b3-machine-qualified-checkpoint` at the exact runtime SHA above
- frozen Owner checkpoint: `rep4-b3-owner-checkpoint` at the exact runtime SHA above
- workflow: `Rep4 B3 Build Play Build`
  - run: `33971984660`
  - job: `101322006240`
  - final workflow result: PASS
  - `npm run check` / source suite + production build: PASS
  - inherited B1 rendered-correspondence Chromium regression: PASS
  - inherited B2 direct-construction Chromium regression: PASS
  - B3 real Chromium causal BUILD → PLAY → BUILD gate: PASS
  - artifact: `rep4-b3-build-play-build`
  - artifact id: `9971217303`
  - artifact SHA-256: `f3004a36a896125e7bb791d3cd5a275aba95af0851803a31a53d01ddae0d7329`

## What B3 earned

B3 closes the bounded causal loop that B2 deliberately left open.

A materially edited BUILD is not converted into hidden tuning parameters or a browser-only animation. On PLAY, the app clones the current authored `Rep4DamperedCornerAuthority`, freezes that copy for the run, and supplies it directly to the existing native `runRep4DamperedCornerProbe(..., "DAMPER", 120)` path. The rendered PLAY mechanism then consumes the resulting native Box3D snapshots through the already-qualified Stage-B projection path.

The real Chromium gate edits two mechanically different classes before PLAY:

1. the chassis tie point through the direct spatial XYZ gizmo;
2. the damper lower eye through exact numeric coordinate authoring.

The gate establishes that the resulting authority differs from baseline, that the native derived tie length equals the edited BUILD tie length, and that the native initial damper length equals the edited BUILD damper span. Therefore the run is causally grounded in the edited geometry rather than merely occurring after an edit in the UI.

During PLAY, authoring is locked and the browser observes a 121-snapshot native trace. Accumulated rendered-observer metrics require material wheel motion and damper excursion while rejecting material invented stretch of the fixed tie relation.

When the trace finishes, the app discards simulation state and reprojects BUILD from the untouched authored authority. The full serialized authority signature must exactly equal the pre-PLAY signature, and the three editable hardpoints plus BUILD tie/damper lengths are checked again. The qualified run returned `returnExact = true`.

## Rendered review

Four qualified screenshots were inspected:

- baseline BUILD;
- materially edited BUILD;
- native PLAY after the edited launch;
- exact edited BUILD return.

The edited construction remains legible. PLAY visibly changes the mechanical pose and damper span while hiding direct-authoring handles, so the moving mechanism is not visually confused with an editable BUILD state. The final screenshot returns to the same edited geometry and re-enables the same selected hardpoint/gizmo.

No new donor-scale, acquisition, overlap, or correspondence confound comparable to the earlier B1/B2 failures was observed.

Known minor presentation defect: while PLAY is already active, the `authority return` status line still says `locked authored state · waiting for PLAY`. The actual phase line says `PLAY`, the controls are locked, the trace reports 121 native snapshots, and the defect does not affect authority, simulation, projection, or machine evidence. It is intentionally recorded rather than introducing another implementation round solely for copy polish before Owner judgement.

## Claim boundary

B3 establishes a bounded representative slice:

**directly edited authored geometry → derived Rep4 mechanism → real native Box3D PLAY → visible observer projection → exact authored BUILD return.**

It does **not** establish:

- good or natural construction feel for the Owner;
- sufficient construction bandwidth for a general JV builder;
- arbitrary topology creation/deletion/reconnection;
- final adaptive-component semantics or intent locks;
- correct real-car suspension kinematics/dynamics;
- tire/contact/road/whole-vehicle behavior;
- that the current visual grammar is product-ready;
- final UI, renderer, physics, asset, or data architecture.

## Natural STOP / next evidence

Do not expand automatically into B4, a full suspension builder, tires, or a vehicle.

The machine apparatus has now answered the technical prerequisite strongly enough. The next high-information gate is **Owner hands-on on the frozen `rep4-b3-owner-checkpoint`**.

The Owner verdict should focus on whether:

- manipulating these hardpoints reads as constructing a mechanism rather than tuning parameters;
- the edited BUILD → PLAY transition makes the cause of motion understandable;
- returning to the exact edited BUILD preserves the feeling of a short build/run/improve loop;
- three different mechanical classes provide enough construction bandwidth to feel meaningfully beyond Rep3;
- the slice is interesting enough to justify a richer next representative problem, rather than more polish of Rep4 itself.
