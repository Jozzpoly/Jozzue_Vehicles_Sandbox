# Nextgen Jozz Vehicle — Fresh Takeover

Date: 2026-09-05

Use this file for a fresh Browser ChatGPT / technical co-orchestrator takeover **after Rep3 Owner closure**.

This is a startup mandate, not a substitute for live verification.

## 1. Re-ground live before acting

Repository:

`Jozzpoly/Jozzue_Vehicles_Sandbox`

At takeover:

1. verify live `main` HEAD;
2. read the canonical spine below in order;
3. verify the preserved Rep3 refs only enough to confirm no newer runtime/evidence contradicts closure;
4. do not assume experimental implementation is on `main`;
5. do not restart Rep3, Rep2 donor archaeology, C1 or earlier closed programs by inertia.

Live source/runtime and reproducible evidence override this handoff for technical facts. Current Owner judgement governs feel, readability, intent and product value.

## 2. Canonical reading order

Read first from live `main`:

1. `README.md`
2. `docs/NEXTGEN_JV_PROJECT_SOUL.md`
3. `docs/NEXTGEN_JV_CURRENT_STATE.md`
4. `docs/NEXTGEN_JV_FRESH_TAKEOVER.md` (this file)
5. `evidence/rep3/REP3_OWNER_CLOSURE_2026-09-05.md`
6. `evidence/rep2/REP2_RESEARCH_CLOSURE_INDEX_2026-09-05.md`

Open the detailed Rep3 contract/Stage-A/Stage-B receipts from the preserved experiment lineage only when a current question requires their exact provenance.

## 3. Expected closure refs

Canonical `main` used as the base for Rep3-closure canonicalization:

`a177569e74fb5e438e1eb3760f97e080f104b091`

Verify live `main`; the merge of this closure documentation will advance it.

Rep3 preserved refs:

- experiment pre-closure head: `experiment/rep3-geometry-derived-hinge-line@798cf235b4550bbfb9725364f4f3d85da0cd8d67`
- Stage-A qualified head: `33c248a6f8991a22f11a92509626d13033458038`
- Stage-B qualified runtime / Owner specimen: `3de78caf3c7c0066b352d62097591b48fce90d20`
- frozen Owner alias: `rep3-stage-b-owner-checkpoint@3de78caf3c7c0066b352d62097591b48fce90d20`
- experiment-closure docs: `experiment/rep3-geometry-derived-hinge-line-closure@1238c6281d67be8b76d8eb4db72b052291135280`

Important inherited Rep2/C1 refs remain in `NEXTGEN_JV_CURRENT_STATE.md` and the Rep2 closure index. Do not merge experimental implementations into `main` merely for convenience.

## 4. Closed research state

### E1

CLOSED. Useful bounded evidence about BUILD/PLAY/recovery and preserved intent; no general builder grammar established.

### V0

CLOSED with technical steering-consequence PASS and Owner readability PASS for bounded research-carrier reuse only.

### R1

CLOSED with bounded positive Owner-backed evidence that direct spatial steering geometry can change real driving consequence and support repeated BUILD → DRIVE → BUILD.

Retained warning:

> privileged automation reachability does not prove human discoverability.

### Rep2

Donor forensics, Stage A, C0a/C0b/C0c and C1 are closed bounded evidence.

C1 strengthened the durable rule:

> **One presented mechanical relation has one authority truth. Physics and visible representation are downstream projections of that same relation.**

No C1 Owner/product/vehicle/architecture PASS exists.

### Rep3

CLOSED at its contractual natural STOP.

Technical result:

`two authored physical mounts → derived pivot/hinge line → native revolute relation → real endpoint motion`

is a bounded technical PASS in the tested hinge class.

Human direct manipulation of both bearings and repeated BUILD → PLAY → BUILD were demonstrated in the first Owner hands-on.

Owner verdict:

- apparatus too raw / too limited / too placeholder-heavy to support meaningful construction-quality or feel judgement;
- construction quality: **NOT EARNED**;
- feel: **NOT EARNED**;
- product acceptance: **NOT EARNED**.

Do not reopen or polish Rep3 merely to manufacture a stronger verdict.

## 5. Workflow correction from Rep3

Retain this rule:

> **Only ask the Owner to judge a quality that the current prototype materially manifests. If the apparatus is too sparse to expose that quality, record NOT EARNED and increase only the representative scope needed for the next question.**

Do not translate this into "always build higher-fidelity prototypes".

For each new Owner gate, state explicitly:

1. what quality the Owner is being asked to judge;
2. what task/context/features must be materially present for that quality to be observable;
3. what can remain stubbed/placeholder without contaminating the judgement;
4. what machine/browser preflight is required first;
5. the natural STOP after the first honest verdict.

A second retained rule:

> **The Owner-facing delivery surface is part of the test setup. New hosting/runtime layers should be qualified separately before a critical feel/readability checkpoint when practical.**

StackBlitz/WebContainers successfully ran the frozen Rep3 browser build and fullscreen removed the IDE-chrome issue. Treat it as a useful auxiliary browser-runner candidate, not canonical infrastructure and not a Rep3 negative result.

## 6. Current frontier — richer representative-problem selection

The next implementation is **not yet selected**.

Do not default to:

- another one-joint microbench;
- full wishbone + tire/contact + vehicle;
- generic builder architecture;
- Cardan merely because a donor exists;
- drivable carrier merely because it is more exciting.

The next selected apparatus must remain bounded **and** cross a minimum Owner-judgement eligibility threshold: enough independent meaningful construction choices and coupled consequences that the Owner can form an intention, test it and revise the mechanism.

### Leading candidate class

**Small multi-relation suspension/mechanism bench**, not a full vehicle.

Current leading shapes:

1. **trailing arm + real damper**
   - reuses qualified revolute + distance-spring seams;
   - authored hinge mount pair + damper eyes + arm/wheel endpoint create several meaningful decisions;
   - lower blast radius;
   - risk: may still be too experientially narrow.

2. **bounded two-arm / upright corner (+ optional damper)**
   - substantially stronger vehicle/mechanical representativeness and construction richness;
   - can reuse recovered one-sided suspension / wheel / damper donors;
   - introduces spherical-joint / multi-link / possible overconstraint stability questions that are not yet qualified on the pinned Rep3 substrate.

Current recommendation: do a **machine-only composition feasibility probe** before freezing the exact topology.

If two-arm/upright is clean enough, prefer it because the extra experiential bandwidth may justify the new seam. If it introduces large solver/authority ambiguity, choose trailing-arm + damper and move on rather than turning the next stage into suspension-solver R&D.

## 7. Immediate next task — S0 composition feasibility / selection probe

Do this before product-facing implementation.

Questions:

- does pinned `box3d.js@0.0.2` expose a usable spherical-joint seam for a bench linkage?
- can two arm bodies + upright move stably enough under representative load without pathological overconstraint/drift?
- can all joint anchors/frames be derived from explicit live hardpoints and independently read back?
- can recovered real donor geometry be projected from the new authority rather than inheriting old socket/rig authority?
- how many independent authored decisions are required before the Owner can create materially different mechanism configurations rather than execute one scripted mutation?

Use the smallest source/API/runtime probe necessary to answer those questions. It may be disposable.

Natural S0 STOP:

- select `two-arm/upright` if the new joint-composition seam is bounded and diagnosable;
- otherwise select `trailing-arm + damper` and stop escalating physics complexity.

Do not ask the Owner to test S0.

## 8. After S0 — freeze a new pre-verdict contract

Before implementing the chosen Owner-facing apparatus, record:

- central unknown;
- authored mechanical authority and what is derived;
- required causal metamorphisms / falsification;
- real donor provenance and what donor semantics are explicitly rejected;
- Owner question and minimum experiential manifestation needed to earn that judgement;
- presentation/delivery preflight;
- explicit non-scope;
- natural STOP after first Owner verdict.

Do not infer a final component/reference/data model from this contract.

## 9. Likely bounded execution shape

### Machine causal-composition gate

Prove before UI polish:

- native relation bodies/anchors/frames correspond to authored hardpoints;
- localized hardpoint edits create expected real motion changes;
- damper installation edits change leverage/body response while authored `k/c/restLength` remain fixed;
- unrelated authority remains unchanged where expected;
- invalid/weird configurations diagnose explicitly and remain permissive unless they threaten solver/program integrity;
- visible real-donor endpoints project live physical authority rather than parallel visual truth.

### Owner-facing gate

Only after machine/browser preflight.

It should let the Owner:

- directly manipulate several recognizable hardpoints/components;
- make materially different configurations without following a single scripted recipe;
- run the mechanism and observe coupled consequences;
- return to BUILD with authored intent preserved;
- use camera/gizmo/numeric affordances naturally;
- see real donor visuals where they materially improve recognition/readability.

The intended Owner question should be closer to:

> "Am I now constructing a recognizable mechanism, forming an intention, predicting roughly what a hardpoint change will do, testing it and revising it?"

—not merely "can I move this point?".

STOP after the first honest Owner verdict. Do not automatically add tire/contact, steering, drivetrain or a drivable chassis.

## 10. Product/research invariants

Protect:

- `build → run → observe → improve → get in and drive`;
- real mechanical causal ownership;
- one live authority truth for a presented mechanism;
- direct spatial construction where mechanically appropriate;
- permissive diagnosis over paternalistic blocking when integrity permits;
- real component semantics rather than hidden outcome-preserving retuning;
- real donors before placeholder gravity where donors are relevant;
- Owner hands-on authority for feel/naturalness/readability/value;
- prototype scope matched to the intended claim;
- evidence-scaled validation rather than ritual.

Do not promote experiment substrate, Three.js, `box3d.js`, current data records, UI or StackBlitz into architecture by reuse inertia.

## 11. Execution routing

Browser ChatGPT is currently the primary JV execution/research surface.

The Owner is temporarily reserving Codex/Work for separate experiments. Therefore:

- do not make JV progress depend on Codex;
- do not hand routine JV implementation/research to Codex by default;
- use Browser ChatGPT + GitHub/web/runtime tools directly;
- ask for the smallest Owner/manual/local action only at a genuine capability boundary.

## 12. Immediate takeover instruction

After live verification and reading the canonical spine, **perform S0 and select the next richer representative mechanism**. Do not reply with a passive recap, do not reopen Rep3 by inertia, and do not ask the Owner what to do next if the verified state matches this handoff.